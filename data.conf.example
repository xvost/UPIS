proxy_login proxy_password proxy_count proxy_type rotation_type(only ipv6/36, simple|castom) rotation(True|False) cron_string(minute,hour)
1st_server's_ipv4 server's_ipv664(gateway) server's_ipv6 root_password
2nd_server's_ipv4 server's_ipv664(gateway) server's_ipv6 root_password
3rd_server's_ipv4 server's_ipv664(gateway) server's_ipv6 root_password